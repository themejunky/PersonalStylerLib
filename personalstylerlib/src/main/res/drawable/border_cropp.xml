<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >
    <!-- This is the stroke you want to define -->
    <stroke android:width="@dimen/default_crop_border_dimenssion" android:color="@color/default_crop_border"/>

    <!-- Optional, round your corners -->
    <corners android:bottomLeftRadius="@dimen/default_crop_border_radius"
        android:topLeftRadius="@dimen/default_crop_border_radius"
        android:bottomRightRadius="@dimen/default_crop_border_radius"
        android:topRightRadius="@dimen/default_crop_border_radius" />
</shape>